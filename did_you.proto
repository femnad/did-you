package didyou;

message Task {
    required string name = 3;
}

message TaskCommand {
    enum Command {
        DO = 0;
        DONE = 1;
        LIST = 2;
    }

    required Command command = 4;

    optional Task task = 2;
}

message TaskList {
    repeated Task tasks = 5;
}